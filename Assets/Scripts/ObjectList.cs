using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class ObjectList : MonoBehaviour {
    int randomNo;


    string[] items = new string[] { "Drink", "Coffee", "Code", "Code", "Cat" };
    string[] places = new string[] { "Nothing", "whA WHY", "bleh", "Nothing", "Nada", "MEOW", "Zilch"};
    // Use this for initialization
  /*  void Start () {
        for(int count = 0; count < 6; count++)
        {
            randomNo = Random.Range(1, 5);
            places[randomNo] = items[count];
        }
        print(places);
    }
	*/
	// Update is called once per frame
	void Update () {
		
	}
}
